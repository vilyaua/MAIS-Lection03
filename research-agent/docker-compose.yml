services:
  research-agent:
    build:
      context: .
      args:
        APP_VERSION: "${APP_VERSION:-0.1.0}"
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./output:/app/output
      - ./logs:/app/logs
